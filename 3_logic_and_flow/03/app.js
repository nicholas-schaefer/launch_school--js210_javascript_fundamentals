/*
Odd Numbers

Write a function that takes a positive integer as an argument,
and logs all the odd numbers from 1 to the passed in number (inclusive).
All numbers should be logged on separate lines.
*/

function oddNumbers(input) {
    // for (let i = 1; i <= input; i+=2){
    //     console.log(i);
    // }

    // let i = 1
    // while (i <= input){
    //     console.log(i);
    //     i +=2
    // }

    // [...Array(input + 1).keys()].forEach((el) =>{
    //     if (el % 2 === 1) console.log(el)
    // })

    // [...Array(input + 1).keys()].forEach((el) =>{
    //     if (el % 2 === 1) console.log(el)
    // })

    // for (const el of [...Array(input + 1).keys()]){
    //     if (el % 2 === 1) console.log(el)
    // }

    // for (let i = 1; i <= input; i+=2){
    //     if ( i % 2 === 0 ) continue;
    //     console.log(i);
    // }

}
let target;
target = 10;
oddNumbers(target);

// target = 11;
// oddNumbers(target);

// target = 1;
// oddNumbers(target);